# Minimal runtime dependencies for the Docker inference image.
# Excludes: notebooks, EDA, training, tuning, monitoring (not needed at inference time).

# Core
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.7.0,<1.8.0  # pin to match training environment for artifact compatibility

# Model (only XGBoost needed for inference — the best model)
xgboost>=2.0.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Serialization
joblib>=1.3.0

# Config
pyyaml>=6.0.0
